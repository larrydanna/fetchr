@model web.Models.AskIndexViewModel

<h2>Bones Thrown</h2>

<p>
    @Html.ActionLink("New Bone", "Create", null, new { @class = "btn btn-primary" })
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Date</th>
            <th>Description</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var ask in Model.Asks)
        {
            <tr>
                <td>@Html.DisplayTextFor(b => ask.DateCreated)</td>
                <td>@Html.DisplayTextFor(b => ask.Description)</td>
                <td>@Html.ActionLink("Edit", "Edit", new { id = ask.Id }, new { @class = "btn btn-primary" })</td>
            </tr>
        }
    </tbody>
</table>
